cc_binary(
    name = "app.elf",
    srcs = ["main.c"],
    additional_linker_inputs = ["link.ld"],
    linkopts = [
        "-nostdlib",
        "-nostartfiles",
        "-Wl,-T,$(location link.ld)",
        "-Wl,--gc-sections",
    ],
    copts = ["-mcpu=cortex-m0", "-mthumb"],
)
